# Dashboard Notes

The production dashboard currently shows:

- Gateway pod availability
- HTTP 200 rate
- Average request latency
- CPU and memory by deployment

Known blind spots:

- No retry count panel
- No token cost panel by tenant
- No workflow queue depth panel
- No cross-tenant access anomaly panel
- No connection pool saturation panel

